{
  "version": 2,
  "name": "nexus-projects-service",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  },
  "installCommand": "npm ci && npx prisma generate",
  "buildCommand": "npm run build",
  "env": {
    "NODE_ENV": "production"
  }
}
